# 루브릭: {업무명} (v0.1)

> normative/ 자산이다. 수정은 PR + golden/run_regression.py 통과 후에만.

## 판정값 (폐쇄 목록)

이 업무의 verdict는 아래 값만 허용된다. 다른 값을 출력하면 스키마 위반은 아니어도 회귀 테스트에서 실패한다.

| verdict | 정의 | 예시 |
|---|---|---|
| `normal` | (정의를 쓰시오) | |
| `anomaly` | | |
| `needs_review` | 아래 판단 불가 조건에 해당 | |

## 판정 기준

(각 판정값을 언제 선택하는지, 관찰 가능한 기준으로 서술)

## 경계 사례 처리 원칙

(예: 기준 A와 B가 충돌하면 A 우선. 수치가 임계값 ±5% 이내면 needs_review.)

## 판단 불가 조건 (escalate: true)

- 필수 입력 필드가 비어 있음
- 근거로 삼을 원문이 존재하지 않음
- (업무별 추가 조건)
